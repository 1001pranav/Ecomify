{
  "name": "@ecomify/queue",
  "version": "1.0.0",
  "description": "Message queue abstraction for Ecomify microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "amqplib": "^0.10.3",
    "@ecomify/types": "*"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.4",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
