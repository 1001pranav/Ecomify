import { ProductList } from '@/features/products';

/**
 * Products Page
 *
 * Main products listing page showing all products with search, filters, and pagination.
 * Uses the ProductList component from features/products.
 */

export default function ProductsPage() {
  return <ProductList />;
}
