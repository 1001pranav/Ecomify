import { SettingsPage } from '@/features/settings';

export const metadata = {
  title: 'Settings | Ecomify Admin',
  description: 'Manage your store settings and preferences',
};

export default function Settings() {
  return <SettingsPage />;
}
